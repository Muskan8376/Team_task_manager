{
  "name": "team-task-manager-root",
  "version": "1.0.0",
  "description": "Root deployment package for Render",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
